<header class="header">
  <div class="container">

    <nav class="header__nav">
      {{#if @site.logo}}
        <a class="header__nav-logo" href="{{@site.url}}"><img src="{{@site.logo}}" alt="{{@site.title}}"/></a>
      {{else}}
        <a class="header__nav-item" href="{{@site.url}}">{{@site.title}}</a>
      {{/if}}

      {{#if @site.navigation}}
        {{navigation}}
      {{/if}}
    </nav>

  </div>
</header>
